# 执行与调度参考实现

这里展示了设备准备、游戏启动、驱动连接、前置步骤、用例执行、重试、超时、数据采集和结果上报如何串联。

其中 `blackbox_runner/` 是已抽取出的独立黑盒 UI 自动化执行器，可直接作为其他项目的起点；`examples/` 提供不含任何项目业务的 YAML 模板，`assets/` 用于项目自行放置模板图。

当前实现与内部任务服务器、包体平台和通知系统耦合较重，只适合作为流程参考。重构时应拆成 `TaskProvider`、`DeviceProvider`、`CaseRunner`、`ArtifactStore` 和 `Notifier`。
